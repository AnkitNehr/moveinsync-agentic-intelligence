# Copy to .env (gitignored) and fill in whichever provider you want.
# All keys are optional — the pipeline runs deterministically without them.

ANTHROPIC_API_KEY=
GEMINI_API_KEY=
SARVAM_API_KEY=
NVIDIA_API_KEY=
